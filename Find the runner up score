if __name__ == '__main__':
    n = int(input())
    arr = list(map(int, input().split()))

    arr = list(set(arr))   # Remove duplicate scores
    arr.sort()             # Sort in ascending order

    print(arr[-2])  
